import { Text } from "react-native";

export function NoRecentView() {
  return (
    <Text className="text-center font-HelveticaRoman text-textLight/70 dark:text-textLight/70">
      لم يتم قراءة شئ مؤخرا
    </Text>
  );
}
